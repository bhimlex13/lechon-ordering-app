PORT=8080
NODE_ENV=development
CLIENT_URL=http://localhost:3000
MONGODB_URI=mongodb://localhost:27017/lechon_app
JWT_SECRET=replace-with-a-long-random-string

# Optional — only if you want real email notifications.
# If left blank, emails are logged to the server console (demo mode).
EMAIL_USER=
EMAIL_PASS=

